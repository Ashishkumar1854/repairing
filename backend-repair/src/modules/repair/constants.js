const { ROLES } = require("../auth/constants");

const REPAIR_ERRORS = Object.freeze({
  CUSTOMER_REQUIRED: "CUSTOMER_REQUIRED",
  CUSTOMER_NOT_FOUND: "CUSTOMER_NOT_FOUND",
  TICKET_NOT_FOUND: "TICKET_NOT_FOUND",
  INVALID_STATUS_TRANSITION: "INVALID_STATUS_TRANSITION",
  STATUS_CONFLICT: "STATUS_CONFLICT",
});

const TICKET_STATUSES = Object.freeze({
  RECEIVED: "RECEIVED",
  DIAGNOSING: "DIAGNOSING",
  ESTIMATE_PENDING: "ESTIMATE_PENDING",
  WAITING_APPROVAL: "WAITING_APPROVAL",
  APPROVED: "APPROVED",
  IN_REPAIR: "IN_REPAIR",
  WAITING_PARTS: "WAITING_PARTS",
  SENT_TO_VENDOR: "SENT_TO_VENDOR",
  READY_FOR_DELIVERY: "READY_FOR_DELIVERY",
  DELIVERED: "DELIVERED",
  CANCELLED: "CANCELLED",
  CLOSED: "CLOSED",
});

const PRIORITIES = Object.freeze({
  LOW: "LOW",
  NORMAL: "NORMAL",
  HIGH: "HIGH",
  URGENT: "URGENT",
});

const REPAIR_PERMISSIONS = Object.freeze({
  CREATE_TICKET: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER, ROLES.FRONT_DESK],
  VIEW_TICKETS: [
    ROLES.OWNER,
    ROLES.ADMIN,
    ROLES.MANAGER,
    ROLES.TECHNICIAN,
    ROLES.FRONT_DESK,
    ROLES.ACCOUNTANT,
  ],
  UPDATE_STATUS: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER, ROLES.TECHNICIAN],
});

module.exports = {
  REPAIR_ERRORS,
  TICKET_STATUSES,
  PRIORITIES,
  REPAIR_PERMISSIONS,
};
