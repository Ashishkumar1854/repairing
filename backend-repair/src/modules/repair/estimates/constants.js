const { ROLES } = require("../../auth/constants");

const ESTIMATE_STATUSES = Object.freeze({
  PENDING: "PENDING",
  DRAFT: "DRAFT",
  SENT: "SENT",
  APPROVED: "APPROVED",
  REJECTED: "REJECTED",
  EXPIRED: "EXPIRED",
  CANCELLED: "CANCELLED",
});

const ESTIMATE_ITEM_TYPES = Object.freeze({
  LABOR: "LABOR",
  SERVICE: "SERVICE",
  PART: "PART",
  CLEANING: "CLEANING",
  VENDOR: "VENDOR",
  OTHER: "OTHER",
});

const ESTIMATE_ACTIONS = Object.freeze({
  CREATED: "ESTIMATE_CREATED",
  APPROVED: "ESTIMATE_APPROVED",
  REJECTED: "ESTIMATE_REJECTED",
  EXPIRED: "ESTIMATE_EXPIRED",
});

const ESTIMATE_ERRORS = Object.freeze({
  TICKET_NOT_FOUND: "TICKET_NOT_FOUND",
  ESTIMATE_NOT_FOUND: "ESTIMATE_NOT_FOUND",
  INVALID_TICKET_STATUS: "INVALID_TICKET_STATUS",
  INVALID_ESTIMATE_STATUS: "INVALID_ESTIMATE_STATUS",
  ESTIMATE_EXPIRED: "ESTIMATE_EXPIRED",
  ESTIMATE_ALREADY_FINALIZED: "ESTIMATE_ALREADY_FINALIZED",
  WORKFLOW_CONFLICT: "WORKFLOW_CONFLICT",
});

const ESTIMATE_PERMISSIONS = Object.freeze({
  CREATE: [ROLES.ADMIN, ROLES.TECHNICIAN],
  VIEW: [
    ROLES.ADMIN,
    ROLES.TECHNICIAN,
  ],
  APPROVE: [ROLES.ADMIN],
  REJECT: [ROLES.ADMIN],
});

module.exports = {
  ESTIMATE_STATUSES,
  ESTIMATE_ITEM_TYPES,
  ESTIMATE_ACTIONS,
  ESTIMATE_ERRORS,
  ESTIMATE_PERMISSIONS,
};
