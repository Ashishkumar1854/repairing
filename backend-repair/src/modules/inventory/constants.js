const { ROLES } = require("../auth/constants");

const INVENTORY_MOVEMENT_TYPES = Object.freeze({
  STOCK_IN: "STOCK_IN",
  STOCK_OUT: "STOCK_OUT",
  CONSUMED: "CONSUMED",
  ADJUSTMENT: "ADJUSTMENT",
  RETURNED: "RETURNED",
  TRANSFERRED: "TRANSFERRED",
});

const INVENTORY_ERRORS = Object.freeze({
  ITEM_NOT_FOUND: "INVENTORY_ITEM_NOT_FOUND",
  ITEM_INACTIVE: "INVENTORY_ITEM_INACTIVE",
  SKU_ALREADY_EXISTS: "INVENTORY_SKU_ALREADY_EXISTS",
  INSUFFICIENT_STOCK: "INSUFFICIENT_STOCK",
  TICKET_NOT_FOUND: "TICKET_NOT_FOUND",
  INVALID_TICKET_STATUS: "INVALID_TICKET_STATUS",
  WORKFLOW_CONFLICT: "WORKFLOW_CONFLICT",
});

const INVENTORY_PERMISSIONS = Object.freeze({
  CREATE_ITEM: [ROLES.ADMIN],
  UPDATE_ITEM: [ROLES.ADMIN],
  VIEW_ITEM: [
    ROLES.ADMIN,
    ROLES.TECHNICIAN,
  ],
  CONSUME_PARTS: [ROLES.ADMIN, ROLES.TECHNICIAN],
});

module.exports = {
  INVENTORY_MOVEMENT_TYPES,
  INVENTORY_ERRORS,
  INVENTORY_PERMISSIONS,
};
