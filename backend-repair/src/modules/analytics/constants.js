const { ROLES } = require("../auth/constants");

const ANALYTICS_PERIODS = Object.freeze({
  TODAY: "today",
  WEEK: "week",
  MONTH: "month",
  CUSTOM: "custom",
});

const ANALYTICS_ERRORS = Object.freeze({
  INVALID_DATE_RANGE: "INVALID_DATE_RANGE",
});

const ANALYTICS_PERMISSIONS = Object.freeze({
  OWNER_DASHBOARD: [ROLES.OWNER],
  ADMIN_DASHBOARD: [ROLES.OWNER, ROLES.ADMIN],
  OPERATIONAL: [ROLES.OWNER, ROLES.ADMIN],
  FINANCIAL: [ROLES.OWNER, ROLES.ADMIN],
  PROFITABILITY: [ROLES.OWNER, ROLES.ADMIN],
  TECHNICIAN: [ROLES.OWNER, ROLES.ADMIN],
  INVENTORY: [ROLES.OWNER, ROLES.ADMIN],
  SLA: [ROLES.OWNER, ROLES.ADMIN],
  CUSTOMER: [ROLES.OWNER, ROLES.ADMIN],
});

module.exports = {
  ANALYTICS_PERIODS,
  ANALYTICS_ERRORS,
  ANALYTICS_PERMISSIONS,
};
