const { ROLES } = require("../auth/constants");

const ANALYTICS_PERIODS = Object.freeze({
  TODAY: "today",
  WEEK: "week",
  MONTH: "month",
  CUSTOM: "custom",
});

const ANALYTICS_ERRORS = Object.freeze({
  INVALID_DATE_RANGE: "INVALID_DATE_RANGE",
});

const ANALYTICS_PERMISSIONS = Object.freeze({
  OWNER_DASHBOARD: [ROLES.OWNER, ROLES.ADMIN],
  OPERATIONAL: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER, ROLES.FRONT_DESK],
  FINANCIAL: [ROLES.OWNER, ROLES.ADMIN, ROLES.ACCOUNTANT],
  PROFITABILITY: [ROLES.OWNER, ROLES.ADMIN, ROLES.ACCOUNTANT],
  TECHNICIAN: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER],
  INVENTORY: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER, ROLES.ACCOUNTANT],
  SLA: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER],
  CUSTOMER: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER, ROLES.ACCOUNTANT],
});

module.exports = {
  ANALYTICS_PERIODS,
  ANALYTICS_ERRORS,
  ANALYTICS_PERMISSIONS,
};
