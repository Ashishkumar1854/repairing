const { ROLES } = require("../auth/constants");

const ASSIGNMENT_EVENT_TYPES = Object.freeze({
  ASSIGNED: "ASSIGNED",
  REASSIGNED: "REASSIGNED",
  UNASSIGNED: "UNASSIGNED",
});

const TECHNICIAN_ACTIVITY_TYPES = Object.freeze({
  ASSIGNMENT_CREATED: "ASSIGNMENT_CREATED",
  ASSIGNMENT_REASSIGNED: "ASSIGNMENT_REASSIGNED",
  ASSIGNMENT_ACCEPTED: "ASSIGNMENT_ACCEPTED",
  DIAGNOSIS_STARTED: "DIAGNOSIS_STARTED",
  REPAIR_STARTED: "REPAIR_STARTED",
  PARTS_CONSUMED: "PARTS_CONSUMED",
  REPAIR_PAUSED: "REPAIR_PAUSED",
  REPAIR_RESUMED: "REPAIR_RESUMED",
  REPAIR_COMPLETED: "REPAIR_COMPLETED",
});

const ASSIGNMENT_ERRORS = Object.freeze({
  TICKET_NOT_FOUND: "ASSIGNMENT_TICKET_NOT_FOUND",
  TECHNICIAN_NOT_FOUND: "ASSIGNMENT_TECHNICIAN_NOT_FOUND",
  TECHNICIAN_INACTIVE: "ASSIGNMENT_TECHNICIAN_INACTIVE",
  ALREADY_ASSIGNED: "ASSIGNMENT_ALREADY_ASSIGNED",
  ACTIVE_ASSIGNMENT_EXISTS: "ACTIVE_ASSIGNMENT_EXISTS",
  ACTIVE_ASSIGNMENT_NOT_FOUND: "ACTIVE_ASSIGNMENT_NOT_FOUND",
  INVALID_TICKET_STATUS: "ASSIGNMENT_INVALID_TICKET_STATUS",
  INVALID_TECHNICIAN_ROLE: "INVALID_TECHNICIAN_ROLE",
  STATUS_CONFLICT: "ASSIGNMENT_STATUS_CONFLICT",
});

const ASSIGNMENT_PERMISSIONS = Object.freeze({
  MANAGE_ASSIGNMENTS: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER],
  VIEW_ASSIGNMENTS: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER, ROLES.TECHNICIAN, ROLES.FRONT_DESK],
  VIEW_TECHNICIAN_QUEUE: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER, ROLES.TECHNICIAN],
});

const MANAGER_ROLES = new Set([ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER]);

module.exports = {
  ASSIGNMENT_EVENT_TYPES,
  TECHNICIAN_ACTIVITY_TYPES,
  ASSIGNMENT_ERRORS,
  ASSIGNMENT_PERMISSIONS,
  MANAGER_ROLES,
};
