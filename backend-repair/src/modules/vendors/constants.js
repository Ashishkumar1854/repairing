const { ROLES } = require("../auth/constants");

const VENDOR_REPAIR_STATUSES = Object.freeze({
  CREATED: "CREATED",
  DISPATCHED: "DISPATCHED",
  IN_PROGRESS: "IN_PROGRESS",
  WAITING_VENDOR_QUOTE: "WAITING_VENDOR_QUOTE",
  COMPLETED: "COMPLETED",
  RETURNED: "RETURNED",
  CANCELLED: "CANCELLED",
});

const VENDOR_COST_STATUSES = Object.freeze({
  NOT_ESTIMATED: "NOT_ESTIMATED",
  ESTIMATED: "ESTIMATED",
  APPROVED: "APPROVED",
  INVOICED: "INVOICED",
  PAID: "PAID",
  CANCELLED: "CANCELLED",
});

const VENDOR_ERRORS = Object.freeze({
  VENDOR_NOT_FOUND: "VENDOR_NOT_FOUND",
  TICKET_NOT_FOUND: "VENDOR_TICKET_NOT_FOUND",
  JOB_NOT_FOUND: "VENDOR_REPAIR_JOB_NOT_FOUND",
  ACTIVE_JOB_EXISTS: "ACTIVE_VENDOR_REPAIR_JOB_EXISTS",
  INVALID_TICKET_STATUS: "VENDOR_INVALID_TICKET_STATUS",
  INVALID_JOB_STATUS: "VENDOR_INVALID_JOB_STATUS",
  WORKFLOW_CONFLICT: "VENDOR_WORKFLOW_CONFLICT",
  VENDOR_INACTIVE: "VENDOR_INACTIVE",
});

const VENDOR_PERMISSIONS = Object.freeze({
  MANAGE_VENDORS: [ROLES.ADMIN],
  VIEW_VENDORS: [ROLES.ADMIN, ROLES.TECHNICIAN],
  DISPATCH_REPAIR: [ROLES.ADMIN, ROLES.TECHNICIAN],
  UPDATE_VENDOR_JOB: [ROLES.ADMIN, ROLES.TECHNICIAN],
  MANAGE_VENDOR_COSTS: [ROLES.ADMIN],
});

const ACTIVE_VENDOR_JOB_STATUSES = [
  VENDOR_REPAIR_STATUSES.CREATED,
  VENDOR_REPAIR_STATUSES.DISPATCHED,
  VENDOR_REPAIR_STATUSES.IN_PROGRESS,
  VENDOR_REPAIR_STATUSES.WAITING_VENDOR_QUOTE,
  VENDOR_REPAIR_STATUSES.COMPLETED,
];

module.exports = {
  VENDOR_REPAIR_STATUSES,
  VENDOR_COST_STATUSES,
  VENDOR_ERRORS,
  VENDOR_PERMISSIONS,
  ACTIVE_VENDOR_JOB_STATUSES,
};
