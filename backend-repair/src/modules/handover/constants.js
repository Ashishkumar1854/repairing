const { ROLES } = require("../auth/constants");

const CUSTODY_HOLDER_TYPES = Object.freeze({
  RECEPTION: "RECEPTION",
  TECHNICIAN: "TECHNICIAN",
  VENDOR: "VENDOR",
  CUSTOMER: "CUSTOMER",
  STORAGE: "STORAGE",
});

const HANDOVER_TYPES = Object.freeze({
  RECEPTION_TO_TECHNICIAN: "RECEPTION_TO_TECHNICIAN",
  TECHNICIAN_TO_RECEPTION: "TECHNICIAN_TO_RECEPTION",
  TECHNICIAN_TO_VENDOR: "TECHNICIAN_TO_VENDOR",
  VENDOR_TO_RECEPTION: "VENDOR_TO_RECEPTION",
  RECEPTION_TO_CUSTOMER: "RECEPTION_TO_CUSTOMER",
  INTERNAL_TRANSFER: "INTERNAL_TRANSFER",
  STORAGE_TRANSFER: "STORAGE_TRANSFER",
});

const HANDOVER_ERRORS = Object.freeze({
  TICKET_NOT_FOUND: "HANDOVER_TICKET_NOT_FOUND",
  INVALID_TRANSFER: "INVALID_CUSTODY_TRANSFER",
  INVALID_HOLDER: "INVALID_CUSTODY_HOLDER",
  STAFF_NOT_FOUND: "HANDOVER_STAFF_NOT_FOUND",
  VENDOR_NOT_FOUND: "HANDOVER_VENDOR_NOT_FOUND",
  NOT_ASSIGNED_TECHNICIAN: "NOT_ASSIGNED_TECHNICIAN",
  WORKFLOW_CONFLICT: "HANDOVER_WORKFLOW_CONFLICT",
});

const HANDOVER_PERMISSIONS = Object.freeze({
  MANAGE_HANDOVERS: [ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER, ROLES.TECHNICIAN, ROLES.FRONT_DESK],
  VIEW_HANDOVERS: [
    ROLES.OWNER,
    ROLES.ADMIN,
    ROLES.MANAGER,
    ROLES.TECHNICIAN,
    ROLES.FRONT_DESK,
    ROLES.ACCOUNTANT,
  ],
});

const OVERRIDE_ROLES = new Set([ROLES.OWNER, ROLES.ADMIN, ROLES.MANAGER]);

module.exports = {
  CUSTODY_HOLDER_TYPES,
  HANDOVER_TYPES,
  HANDOVER_ERRORS,
  HANDOVER_PERMISSIONS,
  OVERRIDE_ROLES,
};
